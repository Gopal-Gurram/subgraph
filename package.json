{
  "name": "qsub",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ qsub",
    "create-local": "graph create --node http://localhost:8020/ qsub",
    "remove-local": "graph remove --node http://localhost:8020/ qsub",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 qsub"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
